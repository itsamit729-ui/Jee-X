# Only for export_questions.py and publish.py (the machine with database access).
# Copy to .env (never commit it). A relative DB_SSL_CA is resolved from this folder.
DATABASE_URL=mysql+pymysql://avnadmin:<password>@<host>.aivencloud.com:<port>/defaultdb
DB_SSL_CA=../ca.pem
